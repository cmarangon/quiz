<?php

return [
    /*
    |--------------------------------------------------------------------------
    | Reaction emojis
    |--------------------------------------------------------------------------
    |
    | The set of emojis players can tap during the answer reveal. Used both by
    | the player button row (blade) and by server-side validation in
    | PlayerScreen::react(), so the two can never drift apart.
    |
    */
    'emojis' => ['😂', '😱', '🔥', '👏', '😍', '🎉', '😭'],
];
